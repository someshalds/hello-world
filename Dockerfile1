pipeline{
     agent any
     stages{
         
         stage('Git clone'){
             
            steps{
                git 'https://github.com/someshalds/hello-world.git'
            }
         }
         stage('maven build'){
             steps{
                 sh 'mvn package'
             }
         }
         stage('Create Dockerimage'){
             steps{
                 sh 'docker build -t someshalds/someshalds4567 .'
             }
         }
     }
}
